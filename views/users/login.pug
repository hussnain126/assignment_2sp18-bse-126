extends ../layout

block content
  h1 Login
  form(action="/login", method="post")
        .form-group
            label(for="email") Email
            input#email(type="text", class="form-control", name="email", value="hassnaindps@gmail.com)
        .form-group
            label(for="password") Password
            input#Price(type="password", class="form-control", name="password", value="samsung713")
        input(type="submit", class="btn btn-info" value="Login")   